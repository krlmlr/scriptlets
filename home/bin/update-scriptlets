#!/bin/sh

set -e

cd ~/git/scriptlets
git fetch --quiet
git diff --shortstat origin/master
git pull --rebase --quiet

if [ "$2" == "--force" -o "$2" == "-f" ]; then
	make force-install --quiet
else
	make --quiet
fi
